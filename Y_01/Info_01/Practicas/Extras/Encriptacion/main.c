/*
El objetivo es cifrar y descifrar mensajes de texto de manera sencilla
utilizando una clave, para garantizar la seguridad de los datos transmitidos, 
de longitud variable de hasta 256 bits.

El programa debe ser capaz de cifrar y descifrar
mensajes de texto de un máximo de 255 caracteres.
Cada carácter debe cifrarse mediante una clave de 8 bits
proporcionada por el usuario o generada de manera
aleatoria. Para el cifrado, se utilizará el operador XOR
para mezclar el mensaje con la clave, y además, se
realizará un desplazamiento de 2 posiciones hacia la
izquierda como paso adicional para aumentar la
seguridad del proceso de cifrado.

El programa debe cumplir con las siguientes condiciones: debe mostrar el mensaje original, el
mensaje cifrado en formato hexadecimal y el mensaje descifrado. El desarrollador debe
implementar al menos dos funciones: cifrar y descifrar. Además, es necesario realizar todas las
validaciones necesarias para asegurar el correcto funcionamiento del programa.


# Pseudocódigo #

*/


#include <stdio.h>
#include "encriptar.h"


int main(){
	
	
	
	return 0;
}
